# 운영 260630 #1·#2 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** #1 "결제 확인 필요"에서 방문일 미래 건 제외(대시보드 카운트=주문목록 탭 일치), #2 협력사 정산 0원 라인에 그룹 합계 회색 표시.

**Architecture:** #1은 `DashboardService` 카운트와 `order_page._matches_status_tab` 필터에 동일한 날짜 조건(미래 방문 제외, 미배정 유지)을 추가. #2는 `PartnerSettlementItemRead`에 그룹 합계 2필드를 추가하고 `partner_settlements` 서비스가 그룹 라인 합으로 채운 뒤, `PartnersPage` 그리드가 0원 칸에 회색 보조표시. 마이그레이션 없음.

**Tech Stack:** FastAPI / SQLAlchemy / pytest, React + TS.

**설계 출처:** `docs/superpowers/specs/2026-07-01-ops-260630-fixes-design.md`. 브랜치 `feature/ops-260630-fixes`(main 분기).

---

## File Structure
- Modify: `backend/app/services/dashboard.py`(#1 count), `backend/app/services/order_page.py`(#1 filter), `backend/app/schemas/partner.py`(#2 DTO), `backend/app/services/partner_settlements.py`(#2 group totals), `frontend/src/features/admin/partners/PartnersPage.tsx`(#2 display), 프론트 settlement item 타입
- Test: `backend/tests/test_ops_260630_fixes.py`

---

## Task 1: #1 — "결제 확인 필요" 미래 방문 제외 (대시보드 카운트 + 주문목록 필터)

**Files:**
- Modify: `backend/app/services/dashboard.py`, `backend/app/services/order_page.py`
- Test: `backend/tests/test_ops_260630_fixes.py`

- [ ] **Step 1: 실패 테스트** — `backend/tests/test_ops_260630_fixes.py`

```python
from datetime import date, timedelta
from uuid import uuid4

from app.core.time import business_today
from app.domain.constants import OrderStatus
from app.domain.payment_status import PaymentStatus
from app.models.order import Order
from app.models.order_group import OrderGroup
from app.services.dashboard import DashboardService


def _order(db, *, scheduled, payment_status=PaymentStatus.UNPAID, status=OrderStatus.SCHEDULE_CONFIRMED):
    group = OrderGroup(
        id=str(uuid4()), customer_token=f"t-{uuid4()}", customer_name="C",
        customer_phone="01000000000", customer_address="A", customer_visible_payment=False,
    )
    db.add(group)
    db.flush()
    o = Order(
        id=str(uuid4()), group_id=group.id, status=status, received_date=date(2026, 1, 1),
        scheduled_date=scheduled, service_name="S", payment_status=payment_status,
        customer_token=group.customer_token, customer_name="C", customer_phone="01000000000",
        customer_address="A",
    )
    db.add(o)
    db.flush()
    return o


def test_payment_check_count_excludes_future_visit(db_session):
    today = business_today()
    _order(db_session, scheduled=today + timedelta(days=7))   # 미래 → 제외
    _order(db_session, scheduled=today)                        # 오늘 → 포함
    _order(db_session, scheduled=today - timedelta(days=3))    # 과거 → 포함
    _order(db_session, scheduled=None)                         # 미배정 → 포함
    db_session.commit()
    summary = DashboardService(db_session).summary()
    assert summary.payment_check_needed == 3  # 미래 1건 제외
```

> 주의: conftest 시드에 기존 주문이 있을 수 있다. 그 경우 위 단언을 "신규 3건이 카운트에 반영(델타)" 형태로 조정하거나, 시드 영향이 없는 `db_session`(빈 시드면 그대로). 구현 시 실제 시드 상태 확인 후 단언 보정(핵심: 미래 건이 카운트에서 빠지는 것).

- [ ] **Step 2: 실패 확인**

Run: `cd backend && python -m pytest tests/test_ops_260630_fixes.py -q`
Expected: FAIL(미래 건이 아직 포함되어 카운트 4).

- [ ] **Step 3: 대시보드 카운트** — `backend/app/services/dashboard.py`

`summary()` 상단에 이미 있는 KST 오늘 변수(예: `current`/`today` — `Order.scheduled_date == current` 라인에서 쓰는 그 변수)를 재사용. `payment_check_needed=self._count(...)` 호출에 날짜 조건 추가:
```python
            payment_check_needed=self._count(
                Order.payment_status.in_(PAYMENT_CHECK_STATUSES),
                Order.status != OrderStatus.CANCELLED,
                (Order.scheduled_date <= current) | Order.scheduled_date.is_(None),
            ),
```
(`current`는 그 파일에서 KST 오늘로 쓰는 변수명에 맞춘다. `PAYMENT_CHECK_STATUSES`는 이미 import됨.)

- [ ] **Step 4: 주문목록 필터** — `backend/app/services/order_page.py` `_matches_status_tab`의 `payment_check` 분기:
```python
        if status_key == "payment_check":
            from app.core.time import business_today
            today = business_today()
            return (
                order.status != OrderStatus.CANCELLED
                and order.payment_status in _PAYMENT_CHECK_STATUSES
                and (order.scheduled_date is None or order.scheduled_date <= today)
            )
```
> `business_today()`를 분기에서 호출(주문 수십~수백 수준이라 무해). 대시보드 카운트와 **동일 기준**(KST 오늘, 미배정 포함). import는 파일 상단으로 올려도 무방.

- [ ] **Step 5: 통과 + 회귀**

Run: `cd backend && python -m pytest tests/test_ops_260630_fixes.py tests/test_reports.py -q`
Expected: PASS(신규 + 기존 그린).

- [ ] **Step 6: 커밋**

```bash
git add backend/app/services/dashboard.py backend/app/services/order_page.py backend/tests/test_ops_260630_fixes.py
git commit -m "fix(orders): 결제 확인 필요에서 방문일 미래 건 제외(카운트=목록 일치)"
```

---

## Task 2: #2 백엔드 — 협력사 정산 그룹 합계

**Files:**
- Modify: `backend/app/schemas/partner.py`, `backend/app/services/partner_settlements.py`
- Test: `backend/tests/test_ops_260630_fixes.py`(추가)

- [ ] **Step 1: 실패 테스트 추가** — `backend/tests/test_ops_260630_fixes.py`

```python
from app.db.seed import DEV_PARTNER_ID
from app.services.partner_settlements import PartnerSettlementService


def test_settlement_item_includes_group_totals(db_session):
    # 한 그룹(고객)에 2라인: 금액은 라인1에만. 둘 다 DEV_PARTNER에 배정·서비스완료·미정산.
    group = OrderGroup(
        id=str(uuid4()), customer_token=f"t-{uuid4()}", customer_name="강남",
        customer_phone="01011112222", customer_address="A", customer_visible_payment=False,
    )
    db_session.add(group)
    db_session.flush()
    common = dict(
        group_id=group.id, status=OrderStatus.COMPLETED, received_date=date(2026, 1, 1),
        service_name="청소", partner_id=DEV_PARTNER_ID, partner_payment_status="unpaid",
        customer_token=group.customer_token, customer_name="강남", customer_phone="01011112222",
        customer_address="A",
    )
    db_session.add(Order(id=str(uuid4()), total_amount=120000, partner_payment_amount=80000, **common))
    db_session.add(Order(id=str(uuid4()), total_amount=0, partner_payment_amount=0, **common))
    db_session.commit()

    result = PartnerSettlementService(db_session).list_settlements(partner_id=DEV_PARTNER_ID, status="unpaid")
    items = [i for i in result.items if i.customer_name == "강남"]
    assert len(items) == 2
    for it in items:
        assert it.group_consumer_total == 120000  # 그룹 합계가 두 라인 모두에 실림
        assert it.group_partner_total == 80000
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && python -m pytest tests/test_ops_260630_fixes.py -k group_totals -q`
Expected: FAIL — `AttributeError: ... 'group_consumer_total'`.

- [ ] **Step 3: DTO** — `backend/app/schemas/partner.py` `PartnerSettlementItemRead`에 추가:
```python
    group_consumer_total: float = 0
    group_partner_total: float = 0
```

- [ ] **Step 4: 서비스** — `backend/app/services/partner_settlements.py`

`list_settlements`에서 그룹별 합계를 계산해 `to_settlement_item`에 전달. `groups_by_id = ...` 다음에:
```python
        group_totals: dict[str, tuple[float, float]] = {}
        for gid in {order.group_id for order in orders}:
            lines = self.groups.list_lines(gid)  # deleted 제외
            consumer = sum(
                (order_consumer_total(line) for line in lines if line.status != OrderStatus.CANCELLED),
                Decimal("0"),
            )
            partner = sum(
                (money_decimal(line.partner_payment_amount) for line in lines if line.status != OrderStatus.CANCELLED),
                Decimal("0"),
            )
            group_totals[gid] = (float(consumer), float(partner))
        items = [
            to_settlement_item(
                order,
                group=groups_by_id.get(order.group_id),
                group_totals=group_totals.get(order.group_id, (0.0, 0.0)),
            )
            for order in orders
        ]
```
`to_settlement_item` 시그니처에 `group_totals: tuple[float, float] = (0.0, 0.0)` 추가하고 반환 DTO에:
```python
        group_consumer_total=group_totals[0],
        group_partner_total=group_totals[1],
```
> `self.groups.list_lines` 는 `OrderGroupRepository.list_lines`(deleted 제외). `order_consumer_total`·`money_decimal`은 이미 그 파일에서 import/정의됨. 취소 라인은 합계 제외.

- [ ] **Step 5: 통과 확인**

Run: `cd backend && python -m pytest tests/test_ops_260630_fixes.py tests/test_role_dtos.py -q`
Expected: PASS(그룹 합계는 admin 정산 DTO 전용 — 협력사/고객 DTO 무관, 회귀 없음).

- [ ] **Step 6: 커밋**

```bash
git add backend/app/schemas/partner.py backend/app/services/partner_settlements.py backend/tests/test_ops_260630_fixes.py
git commit -m "feat(settlement): 정산 item에 그룹(고객) 합계 추가(0원 라인 보조표시용)"
```

---

## Task 3: #2 프론트 — 0원 라인에 그룹 합계 회색 표시

**Files:**
- Modify: `frontend/src/features/admin/partners/PartnersPage.tsx` + settlement item 타입(프론트 partners API 타입)

- [ ] **Step 1: 프론트 타입** — settlement item 타입(예: `api/admin.ts` 또는 partners API의 `PartnerSettlementItem`)에 추가:
```ts
  group_consumer_total: number;
  group_partner_total: number;
```
(실제 타입 정의 위치를 grep로 찾아 추가. `settlements.items`의 `job` 타입.)

- [ ] **Step 2: 그리드 셀** — `PartnersPage.tsx`의 소비자가/도급가 셀(현재 `<GridCell mono>{formatWon(job.consumer_price)}</GridCell>` / `partner_price`)을 교체:
```jsx
                        <GridCell mono>
                          {formatWon(job.consumer_price)}
                          {job.consumer_price === 0 && job.group_consumer_total > 0 && (
                            <div style={{ fontSize: 10.5, color: 'var(--text-tertiary)', fontWeight: 400 }}>
                              그룹 {formatWon(job.group_consumer_total)}
                            </div>
                          )}
                        </GridCell>
                        <GridCell mono>
                          {formatWon(job.partner_price)}
                          {job.partner_price === 0 && job.group_partner_total > 0 && (
                            <div style={{ fontSize: 10.5, color: 'var(--text-tertiary)', fontWeight: 400 }}>
                              그룹 {formatWon(job.group_partner_total)}
                            </div>
                          )}
                        </GridCell>
```

- [ ] **Step 3: 검증** — Run: `cd frontend && npm run typecheck && npm run lint && npm run build` → green.

- [ ] **Step 4: 커밋**

```bash
git add frontend/src/features/admin/partners/PartnersPage.tsx frontend/src/api/admin.ts
git commit -m "feat(settlement): 0원 정산 라인에 그룹 합계 회색 보조표시"
```

---

## Task 4: 최종 검증
- [ ] **Step 1: 백엔드** — `cd backend && python -m pytest -q` 전부 PASS + `python -m compileall app tests`.
- [ ] **Step 2: 프론트** — `cd frontend && npm run typecheck && npm run lint && npm run build` green.

---

## Self-Review
**1. Spec coverage:** #1(카운트+필터)→T1, #2 백엔드(DTO+서비스)→T2, #2 프론트→T3, 검증→T4. ✅
**2. Placeholder scan:** 실제 코드. "실제 변수명/타입 위치 확인"은 정합 지시(공백 아님).
**3. Type consistency:** `group_consumer_total`/`group_partner_total`(T2 DTO) = 서비스가 채움(T2) = 프론트 타입/표시(T3) 일치 ✅. #1 날짜조건은 카운트(T1 Step3)·필터(T1 Step4) 동일(`business_today()`·미배정 포함) ✅.
**주의:** T1 카운트 변수명(`current` 등)·T3 settlement item 타입 위치는 실제 코드 확인 후 맞춤. main 분기 `feature/ops-260630-fixes`, 완료 후 main 머지.
