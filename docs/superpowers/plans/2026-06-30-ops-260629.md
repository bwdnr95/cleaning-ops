# 운영시스템 260629 (결제↔상태 자동전환 + 증빙 일괄변경) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 완납↔최종결제완료(서비스완료) 양방향 자동전환(#2·#3)과 리스트 일괄 '증빙자료' 변경(#4)을 추가한다.

**Architecture:** #2·#3은 `OrderService.update()`에 양방향 자동전환을 추가(요청 기준 트리거·명시값 존중·취소/환불 제외·무한루프 없음). #4는 `OrdersPage`의 기존 일괄 액션 패턴에 증빙(receipt_type/receipt_status) 섹션을 추가(백엔드 무변경). 마이그레이션 없음.

**Tech Stack:** Python 3.11 / FastAPI / SQLAlchemy / pytest, Vite + React 19 + TS.

**설계 출처:** `docs/superpowers/specs/2026-06-30-ops-260629-design.md`. 브랜치 `feature/ops-260629`(main 분기). 규칙 `AGENTS.md`·`.claude/rules/*`.

---

## File Structure
- Modify: `backend/app/services/orders.py`(update() 양방향 자동전환), `frontend/src/features/admin/orders/OrdersPage.tsx`(일괄 증빙)
- Create: `backend/tests/test_payment_status_sync.py`

---

## Task 1: #2·#3 — 결제↔상태 양방향 자동전환 (백엔드)

**Files:**
- Modify: `backend/app/services/orders.py`
- Test: `backend/tests/test_payment_status_sync.py`

- [ ] **Step 1: 실패 테스트** — `backend/tests/test_payment_status_sync.py`

```python
from datetime import date
from uuid import uuid4

from app.domain.constants import OrderStatus
from app.domain.payment_status import PaymentStatus
from app.models.order import Order
from app.models.order_group import OrderGroup
from app.schemas.order import OrderUpdate
from app.services.orders import OrderService


def _order(db, *, status, payment_status=None):
    group = OrderGroup(
        id=str(uuid4()), customer_token=f"t-{uuid4()}", customer_name="C",
        customer_phone="01000000000", customer_address="A", customer_visible_payment=False,
    )
    db.add(group)
    db.flush()
    o = Order(
        id=str(uuid4()), group_id=group.id, status=status, received_date=date(2026, 6, 1),
        service_name="S", payment_status=payment_status, customer_token=group.customer_token,
        customer_name="C", customer_phone="01000000000", customer_address="A",
    )
    db.add(o)
    db.flush()
    return o


def test_paid_auto_completes_from_scheduled(db_session):
    o = _order(db_session, status=OrderStatus.SCHEDULE_CONFIRMED, payment_status=PaymentStatus.UNPAID)
    OrderService(db_session).update(o.id, OrderUpdate(payment_status=PaymentStatus.PAID), actor_user_id="admin")
    db_session.refresh(o)
    assert o.status == OrderStatus.COMPLETED


def test_paid_respects_explicit_status_in_same_request(db_session):
    o = _order(db_session, status=OrderStatus.SCHEDULE_CONFIRMED, payment_status=PaymentStatus.UNPAID)
    OrderService(db_session).update(
        o.id, OrderUpdate(payment_status=PaymentStatus.PAID, status=OrderStatus.SCHEDULE_CONFIRMED),
        actor_user_id="admin",
    )
    db_session.refresh(o)
    assert o.status == OrderStatus.SCHEDULE_CONFIRMED  # #2 미발동


def test_paid_does_not_resurrect_cancelled(db_session):
    o = _order(db_session, status=OrderStatus.CANCELLED, payment_status=PaymentStatus.UNPAID)
    OrderService(db_session).update(o.id, OrderUpdate(payment_status=PaymentStatus.PAID), actor_user_id="admin")
    db_session.refresh(o)
    assert o.status == OrderStatus.CANCELLED


def test_completed_auto_marks_paid(db_session):
    o = _order(db_session, status=OrderStatus.CUSTOMER_DELIVERY_DONE, payment_status=PaymentStatus.UNPAID)
    OrderService(db_session).update(o.id, OrderUpdate(status=OrderStatus.COMPLETED), actor_user_id="admin")
    db_session.refresh(o)
    assert o.payment_status == PaymentStatus.PAID


def test_completed_respects_explicit_payment_in_same_request(db_session):
    o = _order(db_session, status=OrderStatus.CUSTOMER_DELIVERY_DONE, payment_status=PaymentStatus.UNPAID)
    OrderService(db_session).update(
        o.id, OrderUpdate(status=OrderStatus.COMPLETED, payment_status=PaymentStatus.UNPAID),
        actor_user_id="admin",
    )
    db_session.refresh(o)
    assert o.payment_status == PaymentStatus.UNPAID  # #3 미발동


def test_completed_does_not_override_refunded(db_session):
    o = _order(db_session, status=OrderStatus.CUSTOMER_DELIVERY_DONE, payment_status=PaymentStatus.REFUNDED)
    OrderService(db_session).update(o.id, OrderUpdate(status=OrderStatus.COMPLETED), actor_user_id="admin")
    db_session.refresh(o)
    assert o.payment_status == PaymentStatus.REFUNDED
```

- [ ] **Step 2: 실패 확인**

Run: `cd backend && python -m pytest tests/test_payment_status_sync.py -q`
Expected: FAIL (자동전환 로직 없음).

- [ ] **Step 3: 구현** — `backend/app/services/orders.py`

(a) import 추가(상단 `from app.domain.payment_status import PAYMENT_TRACKED_FIELDS` 줄을 확장):
```python
from app.domain.payment_status import PAYMENT_TRACKED_FIELDS, PaymentStatus
```

(b) `update()` 메서드에서 "방문일 지정→일정확정" auto 블록(`order.status = OrderStatus.SCHEDULE_CONFIRMED ... metadata={... "auto": True})`) **바로 아래**에 삽입:
```python
        # #2: 완납(paid) 처리 시 자동 최종결제완료(서비스완료). 같은 요청에서 상태를 직접
        # 지정했으면 존중하고, 취소/이미 완료 건은 건드리지 않는다.
        if (
            changes.get("payment_status") == PaymentStatus.PAID
            and "status" not in changes
            and order.status not in (OrderStatus.CANCELLED, OrderStatus.COMPLETED)
        ):
            from_status = order.status
            order.status = OrderStatus.COMPLETED
            self.timeline.record(
                order_id=order.id,
                actor_user_id=actor_user_id,
                event_type=TimelineEventType.STATUS_CHANGED,
                title="상태 변경",
                description="완납 처리로 자동 최종결제완료(서비스완료) 전환되었습니다.",
                metadata={"from": from_status, "to": OrderStatus.COMPLETED.value, "auto": True},
            )

        # #3: 최종결제완료(서비스완료) 전환 시 자동 완납(paid). 같은 요청에서 결제를 직접
        # 지정했으면 존중하고, 이미 완납/환불 건은 덮어쓰지 않는다.
        if (
            changes.get("status") == OrderStatus.COMPLETED
            and "payment_status" not in changes
            and order.payment_status not in (PaymentStatus.PAID, PaymentStatus.REFUNDED)
        ):
            from_payment = order.payment_status
            order.payment_status = PaymentStatus.PAID
            self.timeline.record(
                order_id=order.id,
                actor_user_id=actor_user_id,
                event_type=TimelineEventType.MEMO_ADDED,
                title="결제/정산 변경",
                description="최종결제완료(서비스완료) 전환으로 자동 완납 처리되었습니다.",
                metadata={
                    "changes": {"payment_status": {"from": from_payment, "to": PaymentStatus.PAID.value}},
                    "auto": True,
                },
            )
```

> 주의: 삽입 위치는 기존 "if "status" in changes ... STATUS_CHANGED" 타임라인 블록 **앞**(또는 뒤든 무방 — 두 자동전환은 `changes` 기준이라 순서 무관). `changes.get("payment_status")`/`changes.get("status")`는 OrderUpdate가 StrEnum 값을 담으므로 `PaymentStatus.PAID`/`OrderStatus.COMPLETED`와 `==` 정상 비교.

- [ ] **Step 4: 통과 + 회귀**

Run: `cd backend && python -m pytest tests/test_payment_status_sync.py tests/test_orders_pagination.py -q`
Expected: PASS(신규 6 + 기존 그린).

- [ ] **Step 5: 커밋**

```bash
git add backend/app/services/orders.py backend/tests/test_payment_status_sync.py
git commit -m "feat(orders): 완납↔최종결제완료 양방향 자동전환(명시값 존중·취소/환불 제외)"
```

---

## Task 2: #4 — 리스트 일괄 '증빙자료' (프론트)

**Files:**
- Modify: `frontend/src/features/admin/orders/OrdersPage.tsx`

기존 일괄 결제상태 변경(`handleBulkPaymentStatusChange`·`bulkPaymentStatus`·payment 버튼·BulkActionPanel payment 섹션)을 **그대로 미러링**해 증빙을 추가한다.

- [ ] **Step 1: import + state** — `OrdersPage.tsx`

상단 import에 추가:
```ts
import { RECEIPT_TYPES, RECEIPT_STATUSES } from '../../../domain/receiptType';
```
컴포넌트 state에 추가(기존 `bulkPaymentStatus` 옆):
```ts
  const [bulkReceiptType, setBulkReceiptType] = React.useState('cash_receipt');
  const [bulkReceiptStatus, setBulkReceiptStatus] = React.useState('issued');
```

- [ ] **Step 2: 핸들러** — `handleBulkPaymentStatusChange` 아래에 추가:
```ts
  const handleBulkReceiptChange = async () => {
    // 발급X(none)면 2차는 '해당없음'으로 강제(백엔드도 정규화하지만 UI 일관성).
    const receiptStatus = bulkReceiptType === 'none' ? 'not_applicable' : bulkReceiptStatus;
    await runSelectedOrdersAction({
      successLabel: '증빙자료를 변경했습니다.',
      execute: (orderId) =>
        updateAdminOrder(orderId, { receipt_type: bulkReceiptType, receipt_status: receiptStatus }),
    });
  };
```

- [ ] **Step 3: 일괄 버튼** — 일괄 버튼 행(결제상태 버튼 `orders-bulk-payment-open` 옆)에 추가:
```jsx
          <button data-testid="orders-bulk-evidence-open" style={bulkActionButton(bulkAction === 'evidence')} onClick={() => setBulkAction(bulkAction === 'evidence' ? null : 'evidence')}>증빙자료</button>
```

- [ ] **Step 4: BulkActionPanel 연결 + 섹션** — `<BulkActionPanel ... />` 호출에 props 추가:
```jsx
          bulkReceiptType={bulkReceiptType}
          setBulkReceiptType={setBulkReceiptType}
          bulkReceiptStatus={bulkReceiptStatus}
          setBulkReceiptStatus={setBulkReceiptStatus}
          onApplyEvidence={() => void handleBulkReceiptChange()}
```
`BulkActionPanel` 함수 시그니처(props 구조분해)에 위 5개 추가하고, payment 섹션(`action === 'payment'`)을 참고해 evidence 섹션 추가:
```jsx
      {action === 'evidence' && (
        <div style={{ display: 'flex', gap: 8, alignItems: 'center', flexWrap: 'wrap' }}>
          <select aria-label="일괄 증빙 유형" value={bulkReceiptType}
            onChange={(e) => setBulkReceiptType(e.target.value)}
            style={{ fontSize: 16, padding: '6px 8px' }} data-testid="orders-bulk-receipt-type">
            {RECEIPT_TYPES.map((t) => <option key={t.value} value={t.value}>{t.label}</option>)}
          </select>
          <select aria-label="일괄 증빙 상태"
            value={bulkReceiptType === 'none' ? 'not_applicable' : bulkReceiptStatus}
            disabled={bulkReceiptType === 'none'}
            onChange={(e) => setBulkReceiptStatus(e.target.value)}
            style={{ fontSize: 16, padding: '6px 8px' }} data-testid="orders-bulk-receipt-status">
            {RECEIPT_STATUSES.map((s) => <option key={s.value} value={s.value}>{s.label}</option>)}
          </select>
          <button className="btn btn--primary btn--sm" data-testid="orders-bulk-receipt-apply"
            onClick={onApplyEvidence}>적용</button>
        </div>
      )}
```
> 실제 `BulkActionPanel`의 기존 섹션(payment) 마크업·버튼 클래스·적용 버튼 패턴에 맞춰 미세 조정(위는 payment 섹션 미러). `RECEIPT_TYPES`/`RECEIPT_STATUSES`는 Step1에서 import.

- [ ] **Step 5: 검증** — Run: `cd frontend && npm run typecheck && npm run lint && npm run build` → green.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/features/admin/orders/OrdersPage.tsx
git commit -m "feat(orders): 리스트 일괄 '증빙자료' 변경(발급X→해당없음)"
```

---

## Task 3: 최종 검증

- [ ] **Step 1: 백엔드** — Run: `cd backend && python -m pytest -q` → 전부 PASS. `python -m compileall app tests` → exit 0.
- [ ] **Step 2: 프론트** — Run: `cd frontend && npm run typecheck && npm run lint && npm run build` → green.
- [ ] **Step 3: (선택) e2e** — 기존 주문 e2e 그린 확인. 인프라 불안정하면 루프 말고 보고.

---

## Self-Review (작성자 체크리스트 결과)

**1. Spec coverage:** §1 #2·#3 백엔드→T1, §2 #4 프론트→T2, §3 테스트→T1·T3, §4 검증→T3. ✅ (#1은 안내, 코드 무변경.)

**2. Placeholder scan:** 실제 코드 포함. T2의 "실제 BulkActionPanel에 맞춰 미세조정"은 기존 payment 섹션 미러 지시(공백 아님).

**3. Type consistency:** `PaymentStatus.PAID`/`OrderStatus.COMPLETED`(StrEnum) = changes 문자열값과 `==`(T1) ✅. `handleBulkReceiptChange`→`updateAdminOrder({receipt_type, receipt_status})`(기존 `UpdateOrderInput`에 필드 존재) ✅. `RECEIPT_TYPES`/`RECEIPT_STATUSES`(receiptType.ts) = 셀렉트 옵션 ✅.

**알려진 실행 주의:**
- T1 자동전환은 `changes` 기준 + "상대 필드 미요청 시에만" 발동 → 무한루프 없음·명시값 존중.
- T2는 기존 일괄 결제 패턴(`runSelectedOrdersAction`·`bulkPaymentStatus`·BulkActionPanel payment 섹션)을 그대로 미러. 실제 OrdersPage 구조에 맞춤.
- main 분기 `feature/ops-260629`에서 작업. 완료 후 main 머지.
